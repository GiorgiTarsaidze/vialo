# Full git-history secret scan — gitleaks v8 via docker
# 2026-08-20T10:15:51Z

$ gitleaks detect --source=/repo --redact   # scans all commits
10:15AM INF 13 commits scanned.
10:15AM INF scanned ~1549614 bytes (1.55 MB) in 392ms
10:15AM INF no leaks found

$ gitleaks detect --no-git   # working tree, including gitignored paths (positive control)
226 findings, 0 in tracked files. All findings are in gitignored paths:
  backend/layer/python/botocore/** and .aws-sam/build/** (AWS SDK example fixtures)
  .tmp/aws-session.sh (local temporary AWS session, gitignored, never committed)
This confirms the scanner detects real credentials and that none are in the repository.

$ .kiro/hooks/validate-repository.sh
repository validation passed
